// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Fixed reservation issues, added multi-week reservation view for admin, split court selector."

// Push to remote repo
git push -u origin master

// Pull changes
git pull

// Pull a large amount of changes that will require overwriting
git fetch origin master
git reset --hard FETCH_HEAD
git clean -df