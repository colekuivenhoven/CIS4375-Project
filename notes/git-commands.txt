// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Minor tweaks to reservation and admin pages."

// Push to remote repo
git push -u origin master

// Pull changes
git pull