// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Refectored admin page to be more efficent. Styling updates."

// Push to remote repo
git push -u origin master

// Pull changes
git pull