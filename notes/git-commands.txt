// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Needed pages added, menu changed, and text messaging test system added"

// Push to remote repo
git push -u origin master

// Pull changes
git pull