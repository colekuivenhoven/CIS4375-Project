// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added a lot of features to the reservation system."

// Push to remote repo
git push -u origin master

// Pull changes
git pull