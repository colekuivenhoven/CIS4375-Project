// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Fixed time selector for customer reservation."

// Push to remote repo
git push -u origin master

// Pull changes
git pull