// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Revised login system. Added full customer registration. Added email validation before login."

// Push to remote repo
git push -u origin master

// Pull changes
git pull