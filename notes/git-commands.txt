// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added 'note' template to reservations. Added data reports and admin reservation app to admin page."

// Push to remote repo
git push -u origin master

// Pull changes
git pull