// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added Reservation Edit/Delete Initial functions"

// Push to remote repo
git push -u origin master

// Pull changes
git pull