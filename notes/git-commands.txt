// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added simplified reservation template for customers, while migrating advanced reservation system over to /reserveadmin"

// Push to remote repo
git push -u origin master

// Pull changes
git pull