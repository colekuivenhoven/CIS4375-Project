// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Updated reservation page to include new reservation types, and a cooresponding legend"

// Push to remote repo
git push -u origin master

// Pull changes
git pull