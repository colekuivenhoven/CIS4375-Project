// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Fixed issue with notes containing quotes. Finished detailed user management page."

// Push to remote repo
git push -u origin master

// Pull changes
git pull

// Pull a large amount of changes that will require overwriting
git fetch origin master
git reset --hard FETCH_HEAD
git clean -df