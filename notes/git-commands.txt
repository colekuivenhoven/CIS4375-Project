// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Fixed test function to connect to AWS server"

// Push to remote repo
git push -u origin master

// Pull changes
git pull