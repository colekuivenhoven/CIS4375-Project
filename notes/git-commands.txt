// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Refectored login/reservation/API code to account for customers and employees. Added initial users list to admin page."

// Push to remote repo
git push -u origin master

// Pull changes
git pull