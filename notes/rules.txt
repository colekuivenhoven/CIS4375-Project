Equipment:
    If ballmachine is being used in another reservation, then the ballmachine is not available to reserve at the same time.

Reservations:
    Fix customer reservation 'auto-timing' feature.
    CONFIRM? Customers can only make 2 reservations per day.
    Allow employee accounts to access the extended reservation page to a somewhat limited extent.
    Display notification to customers when a reservation is successfully made. Will include their reservation data.
    Reservation closures send messsage to user.
    Add num of people to reservation. Max 4 ppl.

Alerts:
    Send SMS alert when a reservation is made.

Login:
    Allow customers to reset their password via email.
    Redirect appropriately if user is not logged in.

Admin:
    Create 3 important reports that fit a pie chart, bar chart, and line chart, respectively.
    Admin should be able to select a user from Users list and edit/update their account.
    Add advanced user page to admin portal.
    Replace logs with feedback list for admin to view.

Reports:
    Add SQL-based reports to reports page.
    Figure out 3 best reports for graphs.

Account:
    Add reset password feature to account page.

Contact:
    Create a contact form that allows customers to send feedback.

Documentation:
    1. Combine System architecture diagram & Network architecture diagram
    2. Infrastructure diagram (local -> github -> aws)
    3. Table descriptions(short)
    4. Post-project operation document(AWS Controls)
    5. Simple User Manual(App Controls)

Questions for Professor:
    DONE Do we need to account for cascade deletes/updates in data dictionary? No.
    
Overall To-do:
    1. DOMAIN NAME!!!
    2. Combine System architecture diagram & Network architecture diagram
    3. Infrastructure diagram (local -> github -> aws)

Defaults Password: P@ssw0rd